<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_generate_report" model="ir.actions.server">
        <field name="name">Generate supply chain report</field>
        <field name="model_id" ref="stock.model_stock_move" />
        <field name="state">code</field>
        <field name="code">
model.search([('warehouse_id', '!=', 'False'), ('state', 'not in', ('draft', 'done', 'cancel')), ('commitment_date', '!=', False)])._compute_qty()
        </field>
    </record>
</odoo>
